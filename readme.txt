WordBash
WordPress Clone Written in Bash

README

First, the Admin code has not been released. (It is too embarassing right at 
the moment; even for me. But basically, all it does is to take Markdown text 
files and converts them into a Text-HTML hybrid format text files.)

Secon, there are no comments in the code ('cept for a few as labels and a 
couple as important explanations). This is by design.

Third, the Code Design is described (and attempts made to explain) in the 
POSTS, not in the code nor the READMEs.

Forth, I forget...